import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'indian_car_rental',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
